@* Redirección de compatibilidad: la sección de estadísticas fue migrada al panel unificado. *@
@page "/admin/estadisticas"
@attribute [Authorize]

@inject NavigationManager Nav

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/admin/dashboard", replace: true);
    }
}
